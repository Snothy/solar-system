[target.wasm32-unknown-unknown]
rustflags = [
    "-C", "link-arg=-zstack-size=4194304",
    "-C", "link-arg=--initial-memory=16777216",
]
# Stack: 4MB (default is 1MB)
# Initial memory: 16MB — gives ~12MB of initial heap so dlmalloc never needs to grow on init
